---
title: Graph-Based Approaches to Learning Epileptogenic Zone Localization Using Stereo-EEG Recordings
title_zh: 基于图的方法学习使用立体脑电图记录进行癫痫发生区定位
authors: "Daniel Wendelken, Brian Ervin, Ravindra Arya, Ali A. Minai"
date: 2026-08-19
pdf: "https://arxiv.org/pdf/2608.18887v1"
tags: ["query:eeg-priors"]
score: 8.0
evidence: 图拓扑作为先验用于基于立体脑电的致痫区源定位。
tldr: "局灶性癫痫的术前评估中，立体脑电(sEEG)记录的致痫区(EZ)定位至关重要。基于静息态功能连接的图模型需选择网络拓扑，但拓扑的影响尚缺乏系统研究。本文对40例患者的sEEG数据，在同一学习模型和留一患者评估下，比较了Dense、解剖/几何先验、预算稀疏化及学习稀疏化，并提出Region-Bridge-c。在约30%边保留率下，Region-Bridge-c取得最高平均PR-AUC 0.371±0.015，较Dense高0.022且边数少69%；Spatial-k也具竞争力。结论：图构建应作为可调设计显式优化，而非固定预处理。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.18887v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 614, \"height\": 1000, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.18887v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1108, \"height\": 544, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.18887v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1305, \"height\": 466, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.18887v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 884, \"height\": 238, \"label\": \"Table\"}]"
motivation: 图模型定位致痫区效果依赖网络拓扑，但拓扑影响尚未被系统研究，需要公平受控的比较来指导图构建。
method: 在40例sEEG数据上，固定学习模型与留一患者评估，控制每节点入边数比较Dense、解剖/几何先验、稀疏化及学习稀疏化，提出Region-Bridge-c。
result: "在约30%边保留的稀疏水平下，Region-Bridge-c取得最高平均精确率-召回率曲线下面积0.371，较Dense图高0.022，且边数减少69%；Spatial-k表现接近，但随机剪枝需要近乎稠密保留。"
conclusion: 图拓扑构建需显式评估与选择，最佳构图因患者而异，固定预处理可能损失定位性能。
---

## 摘要
癫痫发生区（EZ）是个体产生癫痫发作的脑区，是癫痫手术的目标。从立体脑电图（sEEG）记录中定位EZ有助于手术规划，但人工解读耗时且侧重于癫痫发作记录。基于记录脑区之间静息态功能连接的图学习模型是一种有吸引力的替代方案，但关键取决于为模型选择的网络拓扑。我们提出了一项基于图模型的对照研究，旨在探索图拓扑如何影响40名患者静息态sEEG的EZ定位。使用相同的简单可学习模型和留一患者评估，我们比较了稠密图、基于解剖学和几何学的先验、预算稀疏化方法以及学习稀疏化，包括所提出的Region-Bridge-$c$拓扑。为了公平比较图构建，我们控制每个节点的入边数量并改变图的稀疏度。在约30%的边缘保留率下，Region-Bridge-$c$实现了最高的观测平均PR-AUC（$0.371\pm0.015$；ROC-AUC $0.743\pm0.010$），同时使用的边比Dense少约69%（PR-AUC $0.349\pm0.014$）。Spatial-$k$具有竞争力，而随机剪枝则需要接近稠密的保留。学习稀疏化受益于解剖学节点元数据，但平均而言并未超过最佳固定先验。在所有拓扑中，最佳选择因患者而异。这些结果表明，图构建应被明确评估，而非视为固定的预处理。

## Abstract
The epileptogenic zone (EZ) is the brain region that generates seizures in an individual, and is the target of epilepsy surgery. Localizing the EZ from stereo-EEG (sEEG) recordings supports surgical planning, but manual interpretation is time-consuming and focuses on seizure recordings. Graphical learning models of resting-state functional connectivity among the recorded brain regions are an attractive alternative, but depend crucially on the network topology chosen for the model.   We present a controlled study of graph-based models to explore how graph topology affects EZ localization from resting-state sEEG in 40 patients. Using the same simple learnable model and leave-one-patient-out evaluation, we compare dense graphs, anatomy- and geometry-informed priors, budgeted sparsification methods, and learned sparsification, including the proposed Region-Bridge-$c$ topology. To compare graph constructions fairly, we control the number of incoming edges per node and vary graph sparsity.   At $\approx 30\%$ edge retention, Region-Bridge-$c$ achieves the highest observed mean PR-AUC ($0.371\pm0.015$; ROC-AUC $0.743\pm0.010$) while using $\approx 69\%$ fewer edges than Dense (PR-AUC $0.349\pm0.014$). Spatial-$k$ is competitive, whereas random pruning requires near-dense retention. Learned sparsification benefits from anatomical node metadata but, on average, does not surpass the best fixed prior. Across all topologies, the best choice varies by patient. These results suggest that graph construction should be evaluated explicitly rather than treated as fixed preprocessing.

---

## 论文详细总结（自动生成）

好的，遵照您的要求，以下是对该论文的详细中文总结。

---

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在利用立体脑电图（sEEG）进行癫痫发生区（EZ）自动定位的任务中，基于静息态功能连接的图模型性能高度依赖于所选的图拓扑结构（即节点之间如何连接）。然而，这一影响尚未被系统性、公平地评估。本文旨在通过一个受控的对比研究，量化不同图拓扑对EZ定位性能的影响。
- **研究动机**：
  - 癫痫影响全球约5000万人，约30%为药物难治性癫痫，手术是有效治疗手段。
  - sEEG是定位EZ的关键技术，但其临床解读耗时且依赖发作期记录。
  - 基于静息态（发作间期）信号构建功能连接图，并结合图神经网络（GNN）进行学习，是一种有前景的自动化替代方案。
  - 图拓扑的构建（如稠密图、稀疏化策略）会引入不同的先验，显著影响图的统计特性和下游模型的性能，但相关研究往往将其视为固定的预处理步骤，缺乏公平的对照评估。
- **整体含义**：本文通过严谨的实验设计，证明了图拓扑选择是EZ定位模型中的一个核心设计变量，而非无关紧要的预处理步骤。研究提出的受控评估框架和Region-Bridge-c拓扑，为后续研究和临床实践提供了重要参考，强调应根据具体任务和数据显式地评估和选择图构建方法。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：在固定一个简单、统一的骨干学习模型和评估协议的前提下，严格匹配不同图拓扑的稀疏度（每节点入边预算），从而在公平的条件下隔离并研究“图拓扑”这一单一变量对节点分类（EZ vs. non-EZ）性能的影响。
- **关键方法**：
  - **问题建模**：将EZ定位视为图上的二分类问题。每个sEEG电极触点为一个节点，节点特征是解剖标签嵌入；每对触点之间的多条功能连接指标构成边特征。
  - **受控的拓扑比较框架**：为公平比较，所有拓扑（除结构性先验如Dense或区域先验）在特定对比中均控制每个目标节点保留的入边数量 `k = ⌈r·(np-1)⌉`，其中 `np` 为患者触点总数，`r` 为目标边保留率。
  - **骨干模型（Backbone Model）**：
    - **Node Stream**：每个节点根据其MICCAI解剖标签，通过查找表获得一个可学习的嵌入向量 `e_v`。
    - **Edge Stream**：每条边的63维特征向量 `x_uv` 通过一个MLP（`φ_edge`）映射为边嵌入 `m_uv`。
    - **聚合**：通过加权平均聚合目标节点的所有入边信息，得到边特征向量 `h_v`（公式2）。
    - **最终嵌入与分类**：将节点标签嵌入 `e_v` 和边聚合向量 `h_v` 拼接，经过层归一化（LayerNorm）后，输入分类MLP（`φ_cls`）得到节点类别的logits。
  - **提出的新拓扑：Region-Bridge-c**：
    - 结合了解剖先验与几何先验。
    - 首先保留所有在同一粗解剖区域（Region20）内的节点间的边（密集的区域内连接）。
    - 然后为每个节点添加 `b_v = ⌈c·(np-1)⌉` 条距离最近的跨区域边作为“桥梁”。
    - 混合策略在保留局部解剖结构的同时，限制了远距离的跨区域信息流。
  - **学习的稀疏化模块（Learned Topology）**：
    - 使用一个单独的MLP（`φ_score`）为每条边打分 `s_uv`。
    - 利用α-entmax变换（α=1.5）将得分转换为稀疏、非负的软权重 `w_soft`。
    - 在推理时，强制施加硬top-k预算，仅保留得分最高的k条边，并重新归一化权重为 `w_hard`。
    - 训练时加入辅助的预算合规损失（`L_aux`），鼓励软权重满足目标入度预算。
  - **其他拓扑**：包括Dense（稠密基线）、固定结构先验（Intra-Lead、Lead-Chain、Intra-Region）以及预算控制拓扑（Random-k、Spatial-k）。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

- **数据集**：
  - **患者队列**：40名具有良好术后效果（ILAE 1-3级）的局灶性癫痫患者（21男，19女，平均年龄13.3±5.6岁）。
  - **数据内容**：约5分钟的清醒静息态sEEG数据，采样率2048 Hz。总共5282个触点，其中635个（12%）被临床标记为EZ。
  - **数据预处理**：进行了伪迹去除、60Hz工频陷波、共平均参考、分段为0.5s窗口，并使用PySPI工具在每对电极间计算了185种功能连接特征，最终通过特征聚类筛选出63个特征。
  - **标签与定位**：EZ标签基于标准临床实践（视觉分析发作期sEEG）。电极触点通过CT-MRI配准和FASCILE软件映射到MNI标准空间，并分配了解剖标签（MICCAI）和粗粒度区域标签（Region20）。
- **评估基准（Benchmark）**：
  - **评估协议**：留一患者交叉验证（LOPO），每次用一个患者作为测试集，其余患者按80/20划分训练和验证集。每个fold重复5次随机种子。评估指标为PR-AUC（主要）、ROC-AUC、MCC、敏感性、特异性。
  - **对比方法**：在固定骨干模型下，对比了Dense、固定结构先验（Intra-Lead, Lead-Chain, Intra-Region）、预算稀疏化方法（Random-k, Spatial-k）以及学习稀疏化方法（Learned-edges-k, Learned-Region20-k等）。主要对比在约30%边保留率下进行。
- **消融实验**：在Dense拓扑上进行了消融，包括：仅使用边特征（移除节点嵌入）、仅使用节点嵌入（移除边特征）、随机打乱边特征、随机打乱节点元数据。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU 型号、数量、训练时长等）。若未明确说明，也请指出这一点。

- **未明确说明**：论文正文中**完全没有提及**任何关于计算资源的信息，例如GPU型号、数量、训练时间、能源消耗等。
- **推断**：考虑到数据的规模（40名患者，最多197个触点）和模型的复杂度（简单的两层MLP骨干网络），可以推测计算需求相对适中，普通单GPU工作站应足以完成所有实验。但具体的硬件配置和算力细节是缺失的。

### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平

- **实验数量**：
  - **主实验**：对9种拓扑（Dense、Intra-Lead、Lead-Chain、Intra-Region、Region-Bridge-c、Random-k、Spatial-k、Learned-edges-k、Learned-Region20-k）在40个LOPO folds（每个fold进行5次随机种子实验）下进行了评估，结果如Table I所示。
  - **稀疏度敏感性扫描**：所有预算控制和学习拓扑（除Dense和固定结构先验）在20个不同边保留率（r=0.05到1.00）下进行了扫描（Fig. 2）。
  - **消融实验**：在Dense拓扑上进行了4组消融实验（Table II）。
  - **患者级分析**：统计了每种拓扑在40名患者中的最优表现数量及与Dense的性能差异。
- **充分性评估**：
  - **充分**：实验覆盖了多种类型的拓扑（固定先验、随机基线、几何先验、学习先验），并进行了全面的稀疏度扫描和关键消融。LOPO评估和多种子平均提高了结果的可靠性。
  - **客观**：论文特别强调了超参数仅从Spatial-k锚点拓扑中选出并固定，以避免对特定拓扑有利，这保证了比较的客观性。所有拓扑共享完全相同的骨干模型。
  - **公平**：通过严格执行每节点入边预算（匹配稀疏度），确保了不同稀疏化方法（固定/随机/学习）之间的对比是公平的。
  - **局限**：所有实验基于单一中心的40例患者数据，缺乏外部验证。消融实验仅在Dense拓扑上进行，未评估拓扑与特征类型的交互作用。

### 6. 论文的主要结论与发现

- **图拓扑选择显著影响性能**：在固定骨干模型下，不同拓扑的平均PR-AUC差异可达0.065，表明图构建是影响性能的关键设计决策。
- **Region-Bridge-c表现优异**：在约30%边保留率下，区域-桥梁混合先验Region-Bridge-c取得了最高的平均PR-AUC（0.371），比稠密图（Dense）高0.022，且使用的边数减少了约69%，实现了性能和效率的更好权衡。
- **稀疏化有效但依赖策略**：基于空间距离的稀疏化（Spatial-k）和混合先验（Region-Bridge-c）在中等稀疏度下表现良好。而随机剪枝（Random-k）性能严重下降，说明“保留哪些边”比“保留多少边”更重要。
- **学习稀疏化仍需提升**：学习稀疏化方法（如Learned-Region20-k）能够受益于解剖元数据，性能与固定先验相当，但未能在平均性能上超越最优的固定先验（Region-Bridge-c），在硬约束下其表达能力可能受限。
- **患者个体异质性显著**：没有任何单一拓扑在所有患者上表现最优。最佳拓扑因患者而异，提示固定全局拓扑可能不是最优策略，未来可探索患者自适应的拓扑选择方法。

### 7. 优点：方法或实验设计上有哪些亮点

- **严格的受控对比框架**：这是论文最核心的亮点。通过固定骨干模型、统一评估协议、匹配边预算，成功隔离了“图拓扑”这一单一变量，使不同拓扑之间的性能对比非常公平和可信，有效避免了先前研究中因模型或稀疏度不同导致的混淆。
- **提出并验证了有效的结构先验**：提出的Region-Bridge-c拓扑，结合了解剖先验和几何先验，设计简洁、可解释性强且在实验中表现最佳，具有很强的实用价值。
- **全面的稀疏度扫描**：对主要方法进行了系统的稀疏度敏感性分析，揭示了不同拓扑的最优工作点，并指出仅报告单一稀疏度的性能可能产生误导。
- **深入的消融和患者级分析**：通过消融实验明确了边特征和节点元数据各自的重要性；通过患者级分析揭示了最佳拓扑的个体差异，为未来个性化模型指明了方向。
- **客观的实验设置**：刻意使用简单的模型并固定超参数，避免了调优带来的偏差，使得结果更能反映拓扑本身的价值。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **数据与泛化性**：
  - 实验仅基于单一医学中心、40例患者的小规模队列，缺乏外部多中心验证，其结论的普适性有待考察。
  - 所有患者均为癫痫发作间期的静息态数据，未考虑可能影响功能连接的动态变化，也排除了发作期数据。
- **方法局限**：
  - 未报告任何计算资源（如GPU型号、训练时长）的细节，这对于评估方法的实用性和可复现性是一个缺憾。
  - 骨干模型比较简单（固定聚合器+浅层MLP），虽然有助于隔离变量，但可能无法捕捉复杂的非线性关系，更强大的GNN架构可能会改变性能排名。
  - 学习稀疏化方法采用了硬top-k投影，仅依赖全局训练的评分器，在患者异质性高的小数据场景下易过拟合，性能未能超越手工设计的先验。
  - 区域定义（Region20）和空间距离坐标的准确性依赖于电极定位配准流程，定位误差会直接影响所有依赖这些先验的拓扑。
- **潜在偏差风险**：
  - EZ标签基于临床判断，存在固有的观察者间变异性。
  - 论文提及使用了AI工具（ChatGPT）辅助写作，这本身不影响研究内容，但指出了学术写作中AI使用的透明度要求。

（完）
